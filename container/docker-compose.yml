version: "3.8"
services:
  app:
    build:
      context: ..
      dockerfile: container/Dockerfile
    image: ${CONTAINER_IMAGE:-modernization-agent-app}
    environment:
      - APP_PORT=3000
    ports:
      - "3000:3000"
